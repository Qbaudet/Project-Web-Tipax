import knex from '../database/database.js';
/*
const getCheckById = async (id) => {
    const bill = await knex('checks').select('*').where({id_check: id});
    return bill[0];
    }

const getCheck2 = async () => {  
    const id = 2;
    const result = await getCheckById(id);
    console.log(result);
    }

    getCheck2();*/